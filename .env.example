# Root convenience pointer – copy backend/.env.example and frontend/.env.example
# into their respective folders for local development.
#
# Backend:  backend/.env.example  → backend/.env
# Frontend: frontend/.env.example → frontend/.env
#
# Infrastructure (from repo root):
#   docker compose up -d
